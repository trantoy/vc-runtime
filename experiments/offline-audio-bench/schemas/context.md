# Context: offline-audio-bench/schemas

Schema notes for reports emitted by the offline audio benchmark prototype.

These schemas are experiment contracts. Promote them to `vc-bench` only after
the report fields survive real benchmark usage.
