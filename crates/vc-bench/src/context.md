# Context: vc-bench/src

Rust source for production benchmark CLI and library code.

The library owns report types, benchmark execution, and threshold evaluation.
The binary only calls the library CLI entry point.
