# Context: vc-bench/schemas

Schema notes for benchmark reports emitted by `vc-bench`.

Schemas here are production-facing contracts. Changes to existing field names or
units require a new schema version.
